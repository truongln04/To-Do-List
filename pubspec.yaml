name: todo_list_app
description: "ToDo List app for task management."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  fl_chart: ^0.68.0   # vẽ biểu đồ
  sqflite: ^2.3.3     # SQLite
  path: ^1.9.0        # xử lý đường dẫn DB
  flutter_local_notifications: ^17.0.0
  timezone: ^0.9.2
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  # assets:
  #   - assets/images/
